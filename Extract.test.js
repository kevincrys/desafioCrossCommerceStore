

//test('testando Extract', async () => {
  //  jest.setTimeout(10000000)
  //  const dados =  await require('./Extract');
  //  expect(dados).not.toBeUndefined();
  //  expect(dados).not.toBeNull();


//});
